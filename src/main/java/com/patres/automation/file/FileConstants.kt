package com.patres.automation.file

import com.patres.automation.util.fromBundle

object FileConstants {

    val DEFAULT_TMP_FILENAME = fromBundle("rootSchema.defaultFileName")
    const val TMP_EXTENSION = "tmpAb"

}